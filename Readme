app_project

Introduction

This is a simplistic project built using python. It uses a unit test to test for cases.


Getting Started

Instructions on how to get the project running.

Installation

Step-by-step instructions on how to install and set up your project.
To navigate to project directory
cd app-project

Build Automation Instructions:

1. Open a terminal in the project directory.
2. To compile and test the application, run:
python app.py

3. To run unit tests, execute:
python test_app.py


1. Clone the repository:
   ```sh
   https://github.com/Ogunsijianu/app_project.git
2. Github CLI
gh repo clone Ogunsijianu/app_project
